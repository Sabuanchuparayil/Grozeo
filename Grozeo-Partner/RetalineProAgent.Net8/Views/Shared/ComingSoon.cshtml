@{
    ViewData["Title"] = ViewBag.Title ?? "Coming Soon";
}
<div class="container mt-5 text-center">
    <h2>@(ViewBag.Title ?? "Coming Soon")</h2>
    <p class="text-muted">@(ViewBag.Message ?? "This feature is under development.")</p>
</div>
